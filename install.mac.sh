brew install ruby

sudo xcode-select --install # isntall ruby-dev
sudo xcodebuild -license # isntall ruby-dev

sudo gem install jekyll
sudo gem install bundle

brew install libxml2

bundle install --path vendor/bundle # gitignore에 추가해준다.

npm install 
npm install -g gulp gulp-cli
